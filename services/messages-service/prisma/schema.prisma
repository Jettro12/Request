// services/messages-service/prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")  // La misma URL que usan otros servicios
}

model RequestMessage {
  id         String   @id @default(cuid())
  content    String
  senderId   String   @map("senderId")
  receiverId String   @map("receiverId")
  requestId  String   @map("requestId")
  createdAt  DateTime @default(now()) @map("createdAt")

  @@map("request_messages")
}